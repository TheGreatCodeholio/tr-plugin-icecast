#include "pipeline/call_state.h"

// All state behavior lives in headers / atomics. This translation unit exists
// to give the build system something to compile and to host any future
// non-inline definitions.

namespace icecast_bridge {}